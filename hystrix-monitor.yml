server:
  port: 8989

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      

turbine:
  aggregator:
    clusterConfig: 
    appConfig: web-portal,user-webservice,task-webservice,comments-webservice,auth-server,api-gateway,webservice-registry

